Execute this EXACT task. Do not touch anything beyond what’s listed.
If any command fails, PRINT the error and STOP. Do not “fix” or scaffold.

GOAL
Create the corporate management module skeleton in the BFF for corporate admin authentication.

TOUCH ONLY
apps/bff/src/modules/corporate/corporate.module.ts
apps/bff/src/modules/corporate/auth.controller.ts
apps/bff/src/modules/corporate/auth.service.ts
apps/bff/src/app.module.ts

CHANGES
1. **Create `apps/bff/src/modules/corporate/corporate.module.ts`** with:

```ts
import { Module } from '@nestjs/common';
import { AuthController } from './auth.controller';
import { AuthService } from './auth.service';

/**
 * CorporateModule sets up corporate admin APIs under /corporate.
 * It will be extended with additional services and controllers (orgs, subscriptions, wallet) in future iterations.
 */
@Module({
  controllers: [AuthController],
  providers: [AuthService],
})
export class CorporateModule {}
Create apps/bff/src/modules/corporate/auth.controller.ts with:

ts
Copy code
import { Controller, Post, Body, Get, Req } from '@nestjs/common';
import { AuthService } from './auth.service';

/**
 * Handles corporate admin authentication endpoints.
 * TODO: add guards and JWT token extraction when implementing.
 */
@Controller('corporate/auth')
export class AuthController {
  constructor(private readonly authService: AuthService) {}

  @Post('login')
  async login(
    @Body('email') email: string,
    @Body('password') password: string,
  ) {
    return this.authService.login(email, password);
  }

  @Get('me')
  async me(@Req() req: any) {
    return this.authService.getCurrentAdmin(req);
  }
}
Create apps/bff/src/modules/corporate/auth.service.ts with:

ts
Copy code
import { Injectable } from '@nestjs/common';

/**
 * AuthService contains the logic for corporate admin login and retrieving the current admin.
 * Implement the database lookup, password verification, and JWT generation in a later step.
 */
@Injectable()
export class AuthService {
  async login(email: string, password: string) {
    // TODO: query corporate_admins, validate password, sign JWT
    return { message: 'Corporate admin login not yet implemented' };
  }

  async getCurrentAdmin(req: any) {
    // TODO: extract user from request (e.g. via JWT), fetch details from DB
    return { message: 'Get current admin not yet implemented' };
  }
}
Update apps/bff/src/app.module.ts to import and include the new corporate module. At the top of the file, add:

ts
Copy code
import { CorporateModule } from './modules/corporate/corporate.module';
Then, inside the @Module decorator’s imports array, add CorporateModule (alongside any other imported modules). Do not remove existing entries.

RUN & PRINT

ls apps/bff/src/modules/corporate

grep -n "CorporateModule" apps/bff/src/app.module.ts || true

STOP.