{
  "name": "identity",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/identity/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config services/identity/jest.config.ts --coverage",
        "cwd": "."
      }
    }
  },
  "tags": ["type:service", "scope:identity"]
}